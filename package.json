{
  "name": "puzzpool",
  "version": "1.0.0",
  "description": "Distributed Bitcoin keyspace search pool — Express + SQLite backend, single-file dashboard",
  "license": "MIT",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "test": "jest --forceExit --runInBand",
    "lint": "eslint server.js --max-warnings=0"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": ["**/test/**/*.test.js"]
  }
}
